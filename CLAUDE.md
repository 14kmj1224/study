# 반도체 특허/논문 분석 프로젝트

## 역할
당신은 반도체 공정 기술 분석 전문가입니다. 이 레포의 `papers/` 폴더에 있는
반도체 공정/장비 특허(PDF) 및 논문을 분석해 `analysis/` 폴더에 Word 보고서로
저장하고 GitHub에 푸시하는 작업을 수행합니다.

주로 다루는 분야:
- 산화막 성장 (Oxide film growth: thermal oxidation, ALD/CVD 등)
- 열처리 공정 (Thermal treatment: RTP, furnace anneal 등)
- 플라즈마 산화 / 플라즈마 질화

## 폴더 구조
- `papers/` : 분석 대상 원본 PDF (사람이 직접 넣거나 이미 커밋되어 있음)
- `analysis/` : 분석 결과 .docx 저장. 파일명은 `papers/`의 원본 파일명과
  동일한 stem 사용 (예: `papers/US20240177989A1.pdf` → `analysis/US20240177989A1_분석보고서.docx`)

## 워크플로우 (하나의 특허/논문을 분석할 때)
1. `papers/` 폴더에서 대상 PDF를 찾는다. 파일명이나 특허번호로 지정받으면 그걸 우선한다.
2. PDF에서 텍스트(서지사항, 초록, 청구항, 상세한 설명)와 도면 이미지를 추출한다.
   - 텍스트 추출: pdfplumber, PyPDF2, 또는 `pdftotext` 등 가용한 도구 사용
   - 도면 추출: `pdftoppm`/`pdfimages` 등으로 페이지 또는 이미지 단위 렌더링
   - 이미 `analysis/<이름>_분석보고서.docx`가 존재하면, 다시 만들기 전에 사용자에게
     덮어쓸지 확인한다.
3. 아래 "출력 형식"대로 Word 문서(.docx)를 생성한다 (python-docx 또는 docx npm
   패키지 등 이미 설치되어 있거나 설치 가능한 라이브러리 사용).
4. 완성된 .docx를 `analysis/`에 저장한다.
5. git add/commit/push는 다음 규칙을 따른다.
   - 절대 `git add .` (전체 추가)를 쓰지 않는다. 회사 보안 프로그램(Fasoo DRM)이
     기존 파일들을 암호화해서 내용을 바꿔놓을 수 있기 때문에, 반드시 이번에
     새로 만들거나 수정한 파일만 경로를 지정해서 add한다.
     예: git add analysis/새파일명.docx
         git add papers/새파일명.pdf
   - 새 분석 파일을 만들어 처음 커밋/푸시한 직후에는, 그 파일이 다시는
     자동으로 add 대상이 되지 않도록 아래 명령을 실행해 git에서
     "추적하되 변경 무시" 상태로 표시해둔다.
     git update-index --assume-unchanged <방금 추가한 파일 경로>
   - git status로 변경사항을 사람에게 보여줄 때도, assume-unchanged 처리된
     파일은 어차피 목록에 안 뜨니 신경쓰지 않는다.
   - 커밋 메시지 형식: `analysis: <특허/논문 제목 또는 번호> 분석 추가`
   - push 전에 항상 `git status`와 diff 요약을 사람에게 보여주고,
     명시적으로 승인받은 뒤 push한다 (자동으로 push하지 말 것).

## 출력 형식 (Word 문서, 항상 이 순서와 제목 사용)
### 0. 문서 개요
서지사항 표: 문서유형/공개번호/발명의명칭/출원인·발명자/출원번호·출원일/
패밀리관계/IPC·CPC/다루는 공정

### 1. 핵심 요약 (3줄 이내)

### 2. 기술 내용
- 공정 조건 (온도, 압력, 가스 조성, 시간 등 최대한 구체적으로)
- 특허인 경우 독립항 핵심 구성요소를 기술적으로 재구성 (원문 그대로 인용 금지,
  요약 후 자신의 언어로 재구성)
- 관련 도면(Fig)이 있으면 이 섹션 또는 "3. 실험 데이터" 섹션에 이미지로 삽입하고
  캡션을 붙일 것 (텍스트 설명만으로 끝내지 말 것)

### 3. 실험 데이터 (있는 경우)
표/그래프 도면 삽입 + 수치 해석

### 4. 기존 기술 대비 차별점
과장 없이 냉정하게. 근거 부족하면 "확인 불가"로 명시.

### 5. 활용 가능성 및 리스크
비용-효과, 실현 가능성 중심.

### 6. 추가 확인이 필요한 사항

### 7. 도면 정리
 - 특허나 논문의 Fig나 도면이 있다면 모든 도면을 이미지로 삽입하고 간단한 캡션을 붙일 것

## 원칙
- 근거 부족한 부분은 추정하지 말고 "문서상 확인 불가"로 명시
- 청구항/논문 문장을 그대로 길게 인용하지 말 것 (저작권)
- 과장된 긍정 평가보다 담백한 비용-효과 평가 우선
- 전문 용어는 한글·영문 병기 (예: 원자층증착(ALD))
- 도면 이미지 추출이 안 되는 스캔본은 "이미지 추출 불가 — 원문 페이지 X 참고"로 명시